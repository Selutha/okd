# Copy to ~/.rancher-credentials and chmod 0600.
# Token is generated in Rancher UI: User Avatar -> Account & API Keys -> Create API Key.
# Use a no-scope key (cluster-scoped won't be able to enumerate clusters by name).

RANCHER_URL=https://rancher.example.com
RANCHER_TOKEN=token-xxxxx:LongRandomStringFromRancherUI
